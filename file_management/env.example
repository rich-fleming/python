# Example configuration for the portable backup script
# Copy to .env (optional) and export variables in your shell, or pass via CLI.

# Comma-separated absolute or ~-expanded paths
BACKUP_SOURCES="~/Documents,~/Pictures"

# Destination directory (should be on an attached drive or network share you control)
BACKUP_DESTINATION="~/Backups/HostA"

# Optional: path to a notifier binary if you want desktop notifications
# macOS example (if installed via Homebrew):
BACKUP_NOTIFIER="/opt/homebrew/bin/terminal-notifier"

# Optional: override log location (defaults to ./backup.log)
BACKUP_LOGFILE="~/backup/backup.log"
